[module/bluetooth]
type = custom/script
exec = if [ -d "$XDG_CONFIG_HOME" ]; then $XDG_CONFIG_HOME/rofi/scripts/bluetooth.sh --status; else $HOME/.config/rofi/scripts/bluetooth.sh --status; fi
tail = true
click-left = if [ -d "$XDG_CONFIG_HOME" ]; then $XDG_CONFIG_HOME/rofi/scripts/bluetooth.sh; else $HOME/.config/rofi/scripts/bluetooth.sh; fi

format = %{F#cba6f7}󰂯%{F-} <label>
